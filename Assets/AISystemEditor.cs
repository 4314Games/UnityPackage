using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;
[CustomEditor(typeof(AISystem))]
public class AISystemEditor : Editor {
    //bool isPatrollingButton = false;
    //// Use this for initialization
    //public override void OnInspectorGUI()
    //{
    //    DrawDefaultInspector();
    //    AISystem myScript = (AISystem)target;
    //    isPatrollingButton = EditorGUILayout.Toggle("Patrol", isPatrollingButton);
    //    if (isPatrollingButton)
    //    {
    //        myScript.Patrol();
    //    }
    //}
}
